name: Test GitHub Action

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: 21f1003364@ds.study.iitm.ac.in
        run: echo "Hello, world!"
